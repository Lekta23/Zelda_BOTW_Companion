import { View, Text } from 'react-native';
import React from 'react';

type Props = {};

const CreaturesScreen = (props: Props) => {
  return (
    <View>
      <Text>CreaturesScreen</Text>
    </View>
  );
};

export default CreaturesScreen;
